import Skip from 'icons/skip';

export default {
  title: 'Icons/Skip',
  component: Skip,
};

const Template = (args) => <Skip {...args} />;

export const Default = Template.bind({});
Default.args = {
  size: 24,
  color: '#022A4D',
};
