import ZoomIn from 'icons/zoomIn';

export default {
  title: 'Icons/ZoomIn',
  component: ZoomIn,
};

const Template = (args) => <ZoomIn {...args} />;

export const Default = Template.bind({});
Default.args = {
  size: 24,
  color: '#022A4D',
};
