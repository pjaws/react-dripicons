import Lifting from 'icons/lifting';

export default {
  title: 'Icons/Lifting',
  component: Lifting,
};

const Template = (args) => <Lifting {...args} />;

export const Default = Template.bind({});
Default.args = {
  size: 24,
  color: '#022A4D',
};
