import Exit from 'icons/exit';

export default {
  title: 'Icons/Exit',
  component: Exit,
};

const Template = (args) => <Exit {...args} />;

export const Default = Template.bind({});
Default.args = {
  size: 24,
  color: '#022A4D',
};
