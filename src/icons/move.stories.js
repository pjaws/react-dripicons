import Move from 'icons/move';

export default {
  title: 'Icons/Move',
  component: Move,
};

const Template = (args) => <Move {...args} />;

export const Default = Template.bind({});
Default.args = {
  size: 24,
  color: '#022A4D',
};
