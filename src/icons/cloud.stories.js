import Cloud from 'icons/cloud';

export default {
  title: 'Icons/Cloud',
  component: Cloud,
};

const Template = (args) => <Cloud {...args} />;

export const Default = Template.bind({});
Default.args = {
  size: 24,
  color: '#022A4D',
};
