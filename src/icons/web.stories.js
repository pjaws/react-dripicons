import Web from 'icons/web';

export default {
  title: 'Icons/Web',
  component: Web,
};

const Template = (args) => <Web {...args} />;

export const Default = Template.bind({});
Default.args = {
  size: 24,
  color: '#022A4D',
};
