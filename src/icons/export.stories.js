import Export from 'icons/export';

export default {
  title: 'Icons/Export',
  component: Export,
};

const Template = (args) => <Export {...args} />;

export const Default = Template.bind({});
Default.args = {
  size: 24,
  color: '#022A4D',
};
