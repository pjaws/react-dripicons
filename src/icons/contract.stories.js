import Contract from 'icons/contract';

export default {
  title: 'Icons/Contract',
  component: Contract,
};

const Template = (args) => <Contract {...args} />;

export const Default = Template.bind({});
Default.args = {
  size: 24,
  color: '#022A4D',
};
