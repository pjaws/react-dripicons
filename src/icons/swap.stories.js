import Swap from 'icons/swap';

export default {
  title: 'Icons/Swap',
  component: Swap,
};

const Template = (args) => <Swap {...args} />;

export const Default = Template.bind({});
Default.args = {
  size: 24,
  color: '#022A4D',
};
