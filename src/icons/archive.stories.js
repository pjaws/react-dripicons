import Archive from 'icons/archive';

export default {
  title: 'Icons/Archive',
  component: Archive,
};

const Template = (args) => <Archive {...args} />;

export const Default = Template.bind({});
Default.args = {
  size: 24,
  color: '#022A4D',
};
